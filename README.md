# fire_uav 🚁🔥

**fire_uav** — комплекс под Windows / Linux для обнаружения **огня, дыма и людей** с БПЛА + автоматический планировщик « lawn‑mower » маршрутов.

*Базовая модель: **YOLOv11** (кастомные веса)*  
GUI‑демо, CLI‑инструменты, экспорт миссий в QGroundControl.

<p align="center">
  <img src="https://user-images.githubusercontent.com/placeholder/fire_uav_demo.gif" width="560" />
</p>

---

## 📦 Быстрая установка

```bash
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
```

> 🖥️ Если есть CUDA‑GPU — *сначала* поставьте подходящий `torch` whl, затем `pip install -r requirements.txt`.  
> ⚠️ GUI требует `PyQt5`, если нужен только CLI — удалите строчку из `requirements.txt`.


---

## 🚀 Запуск в одну команду

```bash
python -m fire_uav               # меню gui / detect / plan
```
или конкретно:  
```bash
python -m fire_uav gui           
python -m fire_uav detect --model yolov11.pt --video 0 --conf 0.35
python -m fire_uav plan   --aoi-wkt "POLYGON((...))" --gsd 2.5 --out mission.plan
```

* Все JPEG + JSON детекций ложатся в `./artifacts`.  
* `mission.plan` — QGroundControl‑совместимый JSON.

---

## 📁 Структура проекта
```
fire_uav/
├─ core/        # детектор, геометрия, DTO
├─ flight/      # планировщик, экспорт миссий
├─ io/          # запись и передача данных
├─ ui/          # CLI + Qt‑GUI
├─ utils/       # логирование, GUI‑лог‑handler
└─ tests/       # pytest
```

---

## ✨ Ключевые возможности

| Подсистема | Что делает | Файл/модуль |
|------------|------------|-------------|
| **Detection** | YOLOv11 → `Detection` объекты (`class_id`, `confidence`, `bbox`) + фильтр по классам | `fire_uav/core/detection.py` |
| **GUI** | Тёмный PyQt‑интерфейс, FPS, live‑лог, слайдер confidence, выбор модели | `fire_uav/ui/qt/app.py` |
| **Flight‑Planner** | Грид + TSP + разбиение по батареям | `fire_uav/flight/planner.py` |
| **Mission Export** | QGC JSON / KML | `fire_uav/flight/converter.py` |
| **Recorder** | JPEG + JSON(.gz) | `fire_uav/io/recorder.py` |
| **CLI launch** | `python -m fire_uav [gui|detect|plan]` | `fire_uav/__main__.py` |

---

## ⚙️  Конфиг через переменные окружения

`pydantic-settings` читает переменные с префиксом **`FIRE_`**:

| Переменная | По умолчанию | Что делает |
|------------|--------------|-------------|
| `FIRE_YOLO_MODEL` | `best_yolo11.pt` | путь к *.pt* весам |
| `FIRE_YOLO_CONF`  | `0.40` | порог уверенности |
| `FIRE_OUTPUT_ROOT`| `./artifacts` | куда писать JSON/JPG |

Создайте файл `.env` рядом с репо — все ключи подхватятся автоматически.

---

## 🛠️  Разработка

```bash
pytest -q                # все юниты должны быть зелёными
ruff check . --fix       # автоисправление стиля
black .                  # форматирование
```

CI (GitHub Actions) проверяет `pip install -r requirements.txt && pytest` на push.

---

## 📚 Дополнительные материалы
* `HOW_IT_WORKS.md` — краткое описание архитектуры и точек расширения.  